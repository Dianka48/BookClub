import styles from './Main.module.css';

const Main = () => {
  return (
    <div className={styles.container}>
      {/* <img src={main} alt="open book on a table" /> */}
    </div>
  );
};

export default Main;
